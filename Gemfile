source 'https://rubygems.org'

# Only Kamal — this repo is an Elixir/Phoenix app, Ruby is here purely
# to run `bin/kamal`. Keeping the Gemfile mirrors the pattern used by
# the other Stupendous Kamal-deployed apps (dotty_web, macatlas, …).
gem 'kamal', '~> 2.4'
